import { Typography } from '@mui/material';
import Card from '../../components/Card';

const ModeratorTools = () => {
  return (
    <Card>
      <Typography variant='h3'>Moderation Tools</Typography>
    </Card>
  );
};

export default ModeratorTools;
